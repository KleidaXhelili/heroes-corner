{% extends 'base.html.twig' %}

{% block title %}Catégories{% endblock %}

{% block body %}
    <h1>Nos Heroes-Catégories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Mangas</th>
                <td>{{ category.mangas }}</td>
            </tr>
            <tr>
                <th>Comics</th>
                <td>{{ category.comics }}</td>
            </tr>
            <tr>
                <th>BD</th>
                <td>{{ category.bd }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_index') }}">Retour à la liste</a>

    <a href="{{ path('category_edit', {'id': category.id}) }}">Éditer</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
